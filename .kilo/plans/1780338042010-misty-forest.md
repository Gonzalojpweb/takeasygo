# Plan: Rename route from /explore to /app

## Objective
Change the public route of the explore page from `/explore` to `/app` while keeping the API routes under `/api/explore` unchanged.

## Steps

### 1. Directory Rename
- Rename `app/explore` to `app/app`

### 2. Update Public References (Non-API)
Update all occurrences of `/explore` used for client-side navigation, links, or configuration, but NOT:
- API routes (e.g., `/api/explore/...`)
- Internal fetch calls to API (e.g., `fetch('/api/explore/nearby?...')`)
- Strings in comments or logs not used for routing
- The word "explore" in other contexts (variable names, component names, etc.)

#### Configuration Files
- `next.config.ts`: Line 43: `destination: '/explore',` → `destination: '/app',`
- `app/manifest.ts`: Line 8: `start_url: '/explore',` → `start_url: '/app',`
- `public/sw.js`: 
  - Line 92: `url = '/explore',` → `url = '/app',`
  - Line 109: `const url = e.notification.data?.url ?? '/explore',` → `const url = e.notification.data?.url ?? '/app',`

#### Component Files (client-side navigation)
- `components/explore/ExploreClient.tsx`:
  - Line 372: `router.push(`/explore/${r.id}?type=${r.type}`)` → `/app/${r.id}?type=${r.type}`
  - Line 395: Same change
  - Line 444: Same change
- `components/explore/BottomNav.tsx`:
  - Line 9: `{ id: 'home', href: '/explore', ... }` → href: `/app`
  - Line 10: `{ id: 'map', href: '/explore?view=map', ... }` → `/app?view=map`
  - Line 11: `{ id: 'explore', href: '/explore?view=list', ... }` → `/app?view=list`
  - Line 12: `{ id: 'orders', href: '/explore?view=orders', ... }` → `/app?view=orders`
  - Line 13: `{ id: 'profile', href: '/explore/profile', ... }` → `/app/profile`
  - Line 24: `if (tab.id === 'profile') return pathname === '/explore/profile'` → `/app/profile`
  - Line 28: `if (tab.id === 'home') return pathname === '/explore' && ...` → `/app`
- `components/explore/ClubCard.tsx`:
  - Line 38: `onClick={() => router.push(`/explore/profile/club/${tenantSlug}`)}` → `/app/profile/club/${tenantSlug}`
- `components/explore/HomeHeader.tsx`:
  - Line 61: `onClick={() => router.push('/explore/profile')}` → `/app/profile`
- `components/explore/OrdersView.tsx`:
  - Line 96: `onClick={() => router.push('/login?callbackUrl=/explore')}` → `/app`
  - Line 139: `onClick={() => router.push('/explore')}` → `/app`
- `components/explore/RedemptionSuccess.tsx`:
  - Line 42: `const platformUrl = 'https://www.takeasygo.com/explore'` → `https://www.takeasygo.com/app`
- `components/invite/InviteCard.tsx`:
  - Line 10: `const EXPLORE_URL = 'https://takeasygo.com/explore?source=invitacion'` → `https://takeasygo.com/app?source=invitacion`
  - Line 108: `takeasygo.com/explore` → `takeasygo.com/app` (in text content)
- `components/menu/StoreCarousel.tsx`:
  - Line 51: `onClick={() => router.push(`/explore/profile/club/${tenantSlug}?tab=store`)}` → `/app/profile/club/${tenantSlug}?tab=store`
  - Line 72: Same as above
- `components/poweredBy/PoweredByTakeasy.tsx`:
  - Line 31: `href={tenantSlug ? `/explore?ref=${tenantSlug}` : 'https://takeasygo.com/explore'}` → `/app?ref=${tenantSlug}` and `https://takeasygo.com/app`
- `components/tracking/OrderTracker.tsx`:
  - Line 440: `href={`/explore/profile/club/${tenantSlug}`}` → `/app/profile/club/${tenantSlug}`
- `components/admin/PromotionsManager.tsx`:
  - Line 786: `placeholder="ej: /explore/profile/club/{slug}"` → `/app/profile/club/{slug}`
- `lib/reservationNotifications.ts`:
  - Line 243: `url: '/explore',` → `/app`

#### Page Files (inside app/explore, which will become app/app)
- `app/explore/profile/page.tsx` (will become `app/app/profile/page.tsx`):
  - Line 191: `onClick={() => router.push('/explore/orders')}` → `/app/orders`
  - Line 248: `onClick={() => router.push('/explore/profile/clubs')}` → `/app/profile/clubs`
  - Line 258: `onClick={() => router.push(`/explore/profile/club/${club.tenantSlug}`)}` → `/app/profile/club/${club.tenantSlug}`
  - Line 278: `onClick={() => router.push(`/explore/profile/club/${tenantSlug}`)}` → `/app/profile/club/${tenantSlug}`
  - Line 292: `onClick={() => router.push('/explore/profile/clubs')}` → `/app/profile/clubs`
  - Line 365: `router.push('/explore')` → `/app`
- `app/explore/profile/clubs/page.tsx` (will become `app/app/profile/clubs/page.tsx`):
  - Line 41: `router.replace('/explore/profile')` → `/app/profile`
- `app/explore/profile/club/[tenantSlug]/page.tsx` (will become `app/app/profile/club/[tenantSlug]/page.tsx`):
  - Line 72: `router.push('/explore/profile')` → `/app/profile`
  - Line 79: `router.push('/explore')` → `/app`
- `app/explore/[id]/page.tsx` (will become `app/app/[id]/page.tsx`):
  - No direct links to change (links generated in ExploreClient.tsx)
- `app/explore/layout.tsx` (will become `app/app/layout.tsx`):
  - No direct links to change

#### Other Files
- `app/api\[tenant]\loyalty\notifications\proximity\route.ts`:
  - Line 168: `url: \`/explore/profile/club/${tenantSlug}\`,` → `/app/profile/club/${tenantSlug}`

### 3. API Routes (Do NOT Change)
- All files under `app/api/explore/` remain unchanged (internal API routes)
- Internal fetch calls to these API routes (e.g., `fetch('/api/explore/nearby?...')`) remain unchanged

### 4. Validation Steps
After implementation, verify:
1. Development server loads explore page at `/app`
2. API routes under `/api/explore/` still work
3. Navigation between views (home, list, map, orders) functions correctly
4. Links from other site parts (invitations, notifications) point to correct `/app` URL
5. Service worker and manifest updated correctly
6. Root route (`/`) remains functional
7. No broken links or 404 errors in explore-related functionality

### 5. Additional Considerations
- Next.js `app` router allows folder named `app` inside `app` directory (creates route `/app`)
- Ensure root route (`/`) defined by `app/page.tsx` is not broken
- No need to update imports referencing old path unless they reference the page itself (none exist as explore page accessed via routing only)
- Layout file moves with directory rename; no content changes needed unless hardcoded paths present

## Files to Create/Modify Summary
All specific files and lines listed above require changes. API routes under `app/api/explore/` and internal fetch calls to them remain unchanged.